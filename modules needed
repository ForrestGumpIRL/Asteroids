Direct: Write "pip install direct" in Windows search bar.
Panda3d: Write "pip install panda3d" in Windows search bar.
Math: Write "pip install math" in Windows search bar.
Random: Write "pip install random" in Windows search bar.
